# Separate git repository, cloned here for convenience:
#   https://github.com/tekinbicer/autonomous-ct-experimentation.git
# It has its own history and remote and does not belong in this repo.
projects/

# Personal git-identity helpers.  These only run "git config user.name/email",
# but they carry other people's personal addresses and this repository is
# public.  set_francesco is tracked; the rest are deliberately not.
set_alan
set_alberto
set_viktor

# Hand-made backup copies of MEDM screens.  Git history is the backup, and a
# tracked copy only diverges from the screen it was taken from.
*_BAK.adl
